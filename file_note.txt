File Note



Этот оператор создает структурный элемент -s 3,3,square to the output file path_data+'node29out1.txt'

This operator generates a structural element file. -s 19,19,5,disk,square to the output file path_data+'node8108out1.txt'

This operator generates a structural element file. -s 7,7,3,square,square to the output file path_data+'node8117out1.txt'

This operator generates a structural element file. -s 31,31,7,square,square to the output file path_data+'node8123out1.txt'

This operator generates a structural element file. -s 11,11,5,square,square to the output file path_data+'node8130out1.txt'

Этот оператор создает структурный элемент -s 3,3,square to the output file path_data+'node8136out1.txt'

This operator generates a structural element file. -s 3,3,3,square,square to the output file path_data+'node8137out1.txt'

Пороговый фильтр c гистерезисом. from the input file stored in path_data+'-mch-z.tif' to the output file path_data+'node8218out1.tif'

Этот оператор создает структурный элемент -s 13,13,square to the output file path_data+'node8220out1.txt'

Этот оператор создает структурный элемент -s 5,5,square to the output file path_data+'node8230out1.txt'

Этот оператор масштабирует изображение. from the input file stored in path_data+'-mDAPI.tif' to the output file path_data+'node8232out1.tif'

Этот оператор вычисляет размеры изображения как расстояние в пикселях от центра до краев from the input file stored in path_data+'node8232out1.tif' to the output file path_data+'node8233out1.txt'

Этот оператор вычисляет сумму полуразмеров from the input file stored in path_data+'node8233out1.txt' to the output file path_data+'node8234out1.txt'

Этот оператор вычисляет произведение размеров изображений,
представленных как расстояние в пикселях от центра до границ, на константу from the input file stored in path_data+'node8234out1.txt' to the output file path_data+'node8235out1.txt'

Этот оператор вычисляет произведение размеров изображений,
представленных как расстояние в пикселях от центра до границ, на константу from the input file stored in path_data+'node8235out1.txt' to the output file path_data+'node8236out1.txt'

Этот оператор вычисляет разность размеров изображений,
представленных как расстояние в пикселях от центра до границ from the input file stored in path_data+'node8233out1.txt',path_data+'node8236out1.txt' to the output file path_data+'node8237out1.txt'

This operator expands the dimensions of the input image. 
It adds the pixels to the each side of the image. The number 
of pixels is defined in the second input that can be generated 
by the crop operator. from the input file stored in path_data+'node8232out1.tif',path_data+'node8237out1.txt' to the output file path_data+'node8238out1.tif'

This operator rotates image to a given angle (second input) that can be calculated by the rotate operator.


INPUT
1 image:.tif
2 text:.txt
OUTPUT
1 image:.tif from the input file stored in path_data+'node8238out1.tif',path_data+'rotate.txt' to the output file path_data+'node8239out1.tif'

This operator applies the rules to put the image of Drosophila
embryo into standard orientation. These rules are generated by the
align operator. from the input file stored in path_data+'node8239out1.tif',path_data+'-apee-z.txt' to the output file path_data+'node8240out1.tif'

Этот оператор применяет медианный фильтр from the input file stored in path_data+'node8218out1.tif',path_data+'node8220out1.txt' to the output file path_data+'node8219out1.tif'

Этот оператор выравнивает гистограмму с учетом маски from the input file stored in path_data+'node8240out1.tif',path_data+'node8219out1.tif' to the output file path_data+'node15out1.tif'

This operator reduces the size of the input image using pixel subsampling. from the input file stored in path_data+'node15out1.tif' to the output file path_data+'node8107out1.tif'

This operator computes a two dimensional median filter of a structural element over the given image. The structural element comes from the second input. from the input file stored in path_data+'node8107out1.tif',path_data+'node8108out1.txt' to the output file path_data+'node8109out1.tif'

Пороговый фильтр from the input file stored in path_data+'node8109out1.tif' to the output file path_data+'node8110out1.tif',path_data+'node8110out2.txt'

This operator increases the size of the input image using the pixel replication. from the input file stored in path_data+'node8110out1.tif' to the output file path_data+'node8111out1.tif'

Этот оператор выполняет преобразование расстояния. from the input file stored in path_data+'node8111out1.tif' to the output file path_data+'node8217out1.tif'

This operator reduces the size of the input image using pixel subsampling. from the input file stored in path_data+'node8217out1.tif' to the output file path_data+'node8113out1.tif'

This operator computes a two dimensional median filter of a structural element over the given image. The structural element comes from the second input. from the input file stored in path_data+'node8113out1.tif',path_data+'node8108out1.txt' to the output file path_data+'node8114out1.tif'

This operator increases the size of the input image using the pixel replication. from the input file stored in path_data+'node8114out1.tif' to the output file path_data+'node8115out1.tif'

This operator computes a two dimensional median filter of a structural element over the given image. The structural element comes from the second input. from the input file stored in path_data+'node8115out1.tif',path_data+'node8117out1.txt' to the output file path_data+'node8116out1.tif'

Пороговый фильтр from the input file stored in path_data+'node8116out1.tif' to the output file path_data+'node8118out1.tif',path_data+'node8118out2.txt'

Этот оператор выделяет контуры объектов по алгоритму Шена и Кастана (Shen-Castan) from the input file stored in path_data+'node8118out1.tif' to the output file path_data+'node28out1.tif'

Этот оператор заполненяет обведенные контуры from the input file stored in path_data+'node28out1.tif',path_data+'node29out1.txt' to the output file path_data+'node30out1.tif'

Этот оператор вычисляет попиксельный максимум изображений в стэке. from the input file stored in path_data+'node30out1.tif' to the output file path_data+'node8119out1.tif'

Этот оператор сужения from the input file stored in path_data+'node8119out1.tif',path_data+'node29out1.txt' to the output file path_data+'node31out1.tif'

Этот оператор обрезает края изображения так, чтобы все объекты полностью сохранились from the input file stored in path_data+'node31out1.tif' to the output file path_data+'node32out1.tif',path_data+'node32out2.txt'

This operator reduces dimensions of the input image. It crops pixels
from each side of the image. The number of pixels is defined in the
second input that can be generated by the crop
operator. from the input file stored in path_data+'node8240out1.tif',path_data+'node32out1.tif' to the output file path_data+'node8120out1.tif'

This operator reduces dimensions of the input image. It crops pixels
from each side of the image. The number of pixels is defined in the
second input that can be generated by the crop
operator. from the input file stored in path_data+'node30out1.tif',path_data+'node32out1.tif' to the output file path_data+'node8121out1.tif'

This operator multiplies each data point in multiplier image by the corresponding 
data point in input image and returns the product in the corresponding data point of the output image. from the input file stored in path_data+'node8120out1.tif',path_data+'node8121out1.tif' to the output file path_data+'node8122out1.tif'

This operator performs histogram equalization separately for each pixel 
using the neighborhood defined by the structural element from the second input. 
The structural element can be calculated by the strel operator. from the input file stored in path_data+'node8122out1.tif',path_data+'node8123out1.txt' to the output file path_data+'node8177out1.tif'

This operator reduces dimensions of the input image. It crops pixels
from each side of the image. The number of pixels is defined in the
second input that can be generated by the crop
operator. from the input file stored in path_data+'-cDAPI.tif',path_data+'node32out1.tif' to the output file path_data+'node8254out1.tif'

This operator reduces the intensity of salt and pepper noise in an
image using the Crimmins complementary hulling
algorithm~cite{crimmins85. This algorithm smoothes the image by
reducing the magnitude of locally inconsistent pixels, as well as by
increasing the magnitude of pixels in the neighbourhood surrounding
a spike. The spike is defined here as a pixel whose value is
different from its surroundings by more than 2 intensity levels.
Increasing number of iterations of the algorithm can introduce an effect 
of blurring of the image. In the ultimate case all sharp gradients will be 
smoothed down to a magnitude of 2 intensity levels. from the input file stored in path_data+'node8177out1.tif' to the output file path_data+'node8128out1.tif'

This operator computes a two dimensional median filter of a structural element over the given image. The structural element comes from the second input. from the input file stored in path_data+'node8128out1.tif',path_data+'node8130out1.txt' to the output file path_data+'node8129out1.tif'

Этот оператор сужения from the input file stored in path_data+'node8129out1.tif',path_data+'node8230out1.txt' to the output file path_data+'node8229out1.tif'

Этот оператор морфологической реконструкции from the input file stored in path_data+'node8129out1.tif',path_data+'node8229out1.tif' to the output file path_data+'node8228out1.tif'

This operator inverts the grayscale values of all pixels in input image.


INPUT
1 image:.tif
OUTPUT
1 image:.tif from the input file stored in path_data+'node8228out1.tif' to the output file path_data+'node8131out1.tif'

This operator makes a watershed transformation of an input image. The connectivity can be defined as a parameter.


INPUT
1 image:.tif
OUTPUT
1 image:.tif
PARAMETERS
Connectivity - defines the neighborhood: 6 or 26. from the input file stored in path_data+'node8131out1.tif' to the output file path_data+'node8132out1.tif'

This operator multiplies each data point in multiplier image by the corresponding 
data point in input image and returns the product in the corresponding data point of the output image. from the input file stored in path_data+'node8121out1.tif',path_data+'node8132out1.tif' to the output file path_data+'node8158out1.tif'

NAME
gerosion - Erosion.

PROGRAM NAME
prostak -o erosion

DESCRIPTION
This operator does N successive erosions of the input image 
by the structural element from the second input.

USAGE
prostak -o erosion -m <structural element file> -r <number of sweeps> <input binary file name> <output file name>

ProStack command
prostak -m <structural element file> | -s <vertical size, horizontal size, type> -r <number of sweeps> erosion <input file name> <output file name>

INPUT
1 image:.tif
2 text:.txt

OUTPUT
1 image:.tif

PARAMETERS

Repetitions - number of sweeps from the input file stored in path_data+'node8158out1.tif',path_data+'node8137out1.txt' to the output file path_data+'node8144out1.tif'

Замена плоскости.


INPUT
1 image:.tif
2 marker:.tif
OUTPUT
1 image:.tif from the input file stored in path_data+'node8254out1.tif',path_data+'node8144out1.tif' to the output file path_data+'node8244out1.tif'

Этот оператор извлекает количественную информацию from the input file stored in path_data+'node8144out1.tif',path_data+'node8144out1.tif' to the output file path_data+'node8080out1.txt'

This operator multiplies each data point in multiplier image by the corresponding 
data point in input image and returns the product in the corresponding data point of the output image. from the input file stored in path_data+'node8129out1.tif',path_data+'node8144out1.tif' to the output file path_data+'node8134out1.tif'

Этот оператор выделяет контуры объектов по алгоритму Шена и Кастана (Shen-Castan) from the input file stored in path_data+'node8134out1.tif' to the output file path_data+'node8133out1.tif'

Этот оператор заполненяет обведенные контуры from the input file stored in path_data+'node8133out1.tif',path_data+'node8136out1.txt' to the output file path_data+'node8135out1.tif'

This operator multiplies each data point in multiplier image by the corresponding 
data point in input image and returns the product in the corresponding data point of the output image. from the input file stored in path_data+'node8135out1.tif',path_data+'node8158out1.tif' to the output file path_data+'node8181out1.tif'

This operator does successive dilations of the input image by the
structural element supplied as the second input. The structural
element can be calculated by the strel
operator. from the input file stored in path_data+'node8181out1.tif',path_data+'node8137out1.txt' to the output file path_data+'node8176out1.tif'

This operator multiplies each data point in multiplier image by the corresponding 
data point in input image and returns the product in the corresponding data point of the output image. from the input file stored in path_data+'node8122out1.tif',path_data+'node8176out1.tif' to the output file path_data+'node8215out1.tif'

This operator inverts the grayscale values of all pixels in input image.


INPUT
1 image:.tif
OUTPUT
1 image:.tif from the input file stored in path_data+'node8176out1.tif' to the output file path_data+'node8147out1.tif'

This operator multiplies each data point in multiplier image by the corresponding 
data point in input image and returns the product in the corresponding data point of the output image. from the input file stored in path_data+'node8147out1.tif',path_data+'node8144out1.tif' to the output file path_data+'node8146out1.tif'