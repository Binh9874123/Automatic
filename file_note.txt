File Note



Этот оператор создает структурный элемент -s 23,23,square to the output file path_data+'node8out1.txt'

This operator generates a structural element file.


INPUT
0
OUTPUT
1 text:.txt
PARAMETERS
Window width - in pixels,
Window height - in pixels,
Shape - disk or square. -s 13,13,3,square,square to the output file path_data+'node10out1.txt'

This operator generates a structural element file.


INPUT
0
OUTPUT
1 text:.txt
PARAMETERS
Window width - in pixels,
Window height - in pixels,
Shape - disk or square. -s 5,5,3,square,square to the output file path_data+'node13out1.txt'

Этот оператор создает структурный элемент -s 3,3,square to the output file path_data+'node199out1.txt'

Этот оператор создает структурный элемент -s 3,3,square to the output file path_data+'node217out1.txt'

Этот оператор создает изображение структурного элемента from the input file stored in path_data+'node217out1.txt' to the output file path_data+'node229out1.tif'

This operator rotates image to a given angle (second input) that can be calculated by the rotate operator.


INPUT
1 image:.tif
2 text:.txt
OUTPUT
1 image:.tif from the input file stored in path_data+'-cSXL.tif',path_data+'rotate.txt' to the output file path_data+'node306out1.tif'

This operator applies the rules to put the image of Drosophila
embryo into standard orientation. These rules are generated by the
align operator.


INPUT
1 image:.tif
2 text:.txt
OUTPUT
1 image:.tif from the input file stored in path_data+'node306out1.tif',path_data+'-apee-z.txt' to the output file path_data+'node307out1.tif'

This operator reduces dimensions of the input image. It crops pixels
from each side of the image. The number of pixels is defined in the
second input that can be generated by the crop
operator.


INPUT
1 image:.tif
2 text:.txt
OUTPUT
1 image:.tif from the input file stored in path_data+'node307out1.tif',path_data+'crop-sm.txt' to the output file path_data+'node310out1.tif'

Этот оператор локальное выравнивание гистограммы from the input file stored in path_data+'node310out1.tif',path_data+'node8out1.txt' to the output file path_data+'node7out1.tif'

This operator computes a two dimensional median filter of a structural element over the given image. The structural element comes from the second input.


INPUT
1 image:.tif
2 text:.txt
OUTPUT
1 image:.tif
PARAMETERS
Repetitions- number of sweeps from the input file stored in path_data+'node7out1.tif',path_data+'node10out1.txt' to the output file path_data+'node9out1.tif'

This operator computes linear combination. from the input file stored in path_data+'node9out1.tif',path_data+'node310out1.tif' to the output file path_data+'node11out1.tif'

This operator increases the size of the input image using the pixel replication.


INPUT
1 image:.tif
OUTPUT
1 image:.tif
PARAMETERS
Scale - the scale factor. from the input file stored in path_data+'node11out1.tif' to the output file path_data+'node302out1.tif'

Этот оператор нормализует гистограмму from the input file stored in path_data+'node302out1.tif' to the output file path_data+'node202out1.tif'

This operator inverts the grayscale values of all pixels in input image.


INPUT
1 image:.tif
OUTPUT
1 image:.tif from the input file stored in path_data+'node202out1.tif' to the output file path_data+'node12out1.tif'

NAME
gerosion - Erosion.

PROGRAM NAME
prostak -o erosion

DESCRIPTION
This operator does N successive erosions of the input image 
by the structural element from the second input.

USAGE
prostak -o erosion -m <structural element file> -r <number of sweeps> <input binary file name> <output file name>

ProStack command
prostak -m <structural element file> | -s <vertical size, horizontal size, type> -r <number of sweeps> erosion <input file name> <output file name>

INPUT
1 image:.tif
2 text:.txt

OUTPUT
1 image:.tif

PARAMETERS

Repetitions - number of sweeps from the input file stored in path_data+'node12out1.tif',path_data+'node13out1.txt' to the output file path_data+'node14out1.tif'

NAME
gerosion - Erosion.

PROGRAM NAME
prostak -o erosion

DESCRIPTION
This operator does N successive erosions of the input image 
by the structural element from the second input.

USAGE
prostak -o erosion -m <structural element file> -r <number of sweeps> <input binary file name> <output file name>

ProStack command
prostak -m <structural element file> | -s <vertical size, horizontal size, type> -r <number of sweeps> erosion <input file name> <output file name>

INPUT
1 image:.tif
2 text:.txt

OUTPUT
1 image:.tif

PARAMETERS

Repetitions - number of sweeps from the input file stored in path_data+'node202out1.tif',path_data+'node13out1.txt' to the output file path_data+'node15out1.tif'

This operator performs morphological reconstruction of the image from the marker image.


INPUT
1 image:.tif
2 marker:.tif
OUTPUT
1 image:.tif from the input file stored in path_data+'node202out1.tif',path_data+'node15out1.tif' to the output file path_data+'node16out1.tif'

This operator performs morphological reconstruction of the image from the marker image.


INPUT
1 image:.tif
2 marker:.tif
OUTPUT
1 image:.tif from the input file stored in path_data+'node12out1.tif',path_data+'node14out1.tif' to the output file path_data+'node17out1.tif'

This operator inverts the grayscale values of all pixels in input image.


INPUT
1 image:.tif
OUTPUT
1 image:.tif from the input file stored in path_data+'node17out1.tif' to the output file path_data+'node18out1.tif'

This operator multiplies each data point in multiplier image by the corresponding 
data point in input image and returns the product in the corresponding data point of the output image.


INPUT
1 image:.tif
2 image:.tif
OUTPUT
1 image:.tif from the input file stored in path_data+'node16out1.tif',path_data+'node18out1.tif' to the output file path_data+'node19out1.tif'

This operator computes linear combination. from the input file stored in path_data+'node16out1.tif',path_data+'node18out1.tif' to the output file path_data+'node20out1.tif'

Этот оператор вычисляет попиксельный максимум from the input file stored in path_data+'node20out1.tif',path_data+'node19out1.tif' to the output file path_data+'node21out1.tif'

This operator reduces the intensity of salt and pepper noise in an
image using the Crimmins complementary hulling
algorithm~cite{crimmins85. This algorithm smoothes the image by
reducing the magnitude of locally inconsistent pixels, as well as by
increasing the magnitude of pixels in the neighbourhood surrounding
a spike. The spike is defined here as a pixel whose value is
different from its surroundings by more than 2 intensity levels.
Increasing number of iterations of the algorithm can introduce an effect 
of blurring of the image. In the ultimate case all sharp gradients will be 
smoothed down to a magnitude of 2 intensity levels.


INPUT
1 image:.tif
OUTPUT
1 image:.tif
PARAMETERS
Iterations - number of sweeps. from the input file stored in path_data+'node21out1.tif' to the output file path_data+'node22out1.tif'

Этот оператор локальное выравнивание гистограммы from the input file stored in path_data+'node22out1.tif',path_data+'node199out1.txt' to the output file path_data+'node198out1.tif'

This operator implements the Canny algorithm for edge detection.

INPUT
1 image:.tif
OUTPUT
1 image:.tif
PARAMETERS
The thresholds range from 0 to 100% (e.g. -canny 0x1+10%+30%) with {+lower-percent} < {+upper-percent}. If {+upper-percent} is increased but {+lower-percent} remains the same, lesser edge components will be detected, but their lengths will be the same. If {+lower-percent} is increased but {+upper-percent} is the same, the same number of edge components will be detected but their lengths will be shorter. The default thresholds are shown. The radiusxsigma controls a gaussian blur applied to the input image to reduce noise and smooth the edges. from the input file stored in path_data+'node198out1.tif' to the output file path_data+'node209out1.tif'

Этот оператор заполненяет обведенные контуры from the input file stored in path_data+'node209out1.tif',path_data+'node217out1.txt' to the output file path_data+'node216out1.tif'

This operator reduces the size of the input image using pixel subsampling. 


INPUT
1 image:.tif
OUTPUT
1 image:.tif
PARAMETERS
Scale - the scaling factor from 0 to 1. from the input file stored in path_data+'node22out1.tif' to the output file path_data+'node304out1.tif'

Этот оператор размыкания. from the input file stored in path_data+'node216out1.tif',path_data+'node217out1.txt' to the output file path_data+'node305out1.tif'

Этот оператор выполняет преобразование расстояния. from the input file stored in path_data+'node305out1.tif' to the output file path_data+'node218out1.tif'

This operator inverts the grayscale values of all pixels in input image.


INPUT
1 image:.tif
OUTPUT
1 image:.tif from the input file stored in path_data+'node218out1.tif' to the output file path_data+'node219out1.tif'

Сегментация двумерных изображений методом водоотлива.
Области изображения, соответствующие объектам, помечаются
единицей и разделяются линиями границы, толщиной в один
пиксел и помеченными нулем. from the input file stored in path_data+'node219out1.tif' to the output file path_data+'node220out1.tif'

This operator multiplies each data point in multiplier image by the corresponding 
data point in input image and returns the product in the corresponding data point of the output image.


INPUT
1 image:.tif
2 image:.tif
OUTPUT
1 image:.tif from the input file stored in path_data+'node220out1.tif',path_data+'node305out1.tif' to the output file path_data+'node221out1.tif'

В сегментированном изображении 
могут присутствовать объекты различной природы, а также артефакты.
С другой стороны, некоторые характеристики интересующих объектов,
как то: форма или характерный размер, могут быть известны
a priori и позволяют создать шаблон объекта, что позволяет
найти в изображении наиболее близкие по своим характеристикам
объекты. Объекты, не соответствующие шаблону удаляются из изображения.
Объект может быть охарактеризован набором моментов и инвариантов
сдвига, растяжения и поворота, тогда сходство объектов определяется
как евклидово расстояние между их векторами харатеристик. Если расстояние
меньше заданной точности, то объекты считаются сходными. from the input file stored in path_data+'node221out1.tif',path_data+'node229out1.tif' to the output file path_data+'node228out1.tif'

This operator reduces the size of the input image using pixel subsampling. 


INPUT
1 image:.tif
OUTPUT
1 image:.tif
PARAMETERS
Scale - the scaling factor from 0 to 1. from the input file stored in path_data+'node228out1.tif' to the output file path_data+'node303out1.tif'

Этот оператор извлекает количественную информацию from the input file stored in path_data+'node304out1.tif',path_data+'node303out1.tif' to the output file path_data+'node314out1.txt'

Этот оператор добавляет номер объекта во 2й маске в данные по первой from the input file stored in path_data+'node303out1.tif',path_data+'-cellmask.tif',path_data+'node314out1.txt' to the output file path_data+'node315out1.txt'

Этот оператор извлекает количественную информацию в csv формате from the input file stored in path_data+'node315out1.txt' to the output file path_data+'node316out1.csv'

Этот оператор извлекает количественную информацию from the input file stored in path_data+'node303out1.tif',path_data+'-cellmask.tif' to the output file path_data+'node273out1.txt'

Этот оператор извлекает количественную информацию from the input file stored in path_data+'node304out1.tif',path_data+'-cellmask.tif',path_data+'node273out1.txt' to the output file path_data+'node313out1.txt'

Этот оператор извлекает количественную информацию в csv формате from the input file stored in path_data+'node313out1.txt' to the output file path_data+'node264out1.csv'